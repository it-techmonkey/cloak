"use client";

// Approval workflow removed — all venues are now auto-approved on creation.
// This component returns null since approvalStatus is always "approved".

export default function ApprovalBanner() {
  return null;
}
