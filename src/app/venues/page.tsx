import VenuesPage from "@/components/marketing/VenuesPage";

export const metadata = {
  title: "For Venues — Cloak",
  description: "Digital cloakroom management for nightclubs, event spaces, and hospitality venues. Replace paper tickets with QR passes.",
};

export default function Page() {
  return <VenuesPage />;
}
